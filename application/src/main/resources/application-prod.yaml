spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update

logging:
  level:
    space.harbour.kotlin: INFO
    org.springframework.web: WARN

management:
  endpoint:
    health:
      show-details: when-authorized
