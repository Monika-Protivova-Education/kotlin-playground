apiVersion: v1
kind: Service
metadata:
  name: kotlin-playground
  namespace: kotlin-playground
spec:
  type: ClusterIP
  selector:
    app: kotlin-playground
  ports:
    - port: 8080
      targetPort: 8080
      name: http
